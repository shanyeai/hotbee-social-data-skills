# Security Policy

Use GitHub private vulnerability reporting for security issues. Never include a real `HOTBEE_API_KEY`, `HOTBEE_DOUYIN_KEY`, private media URL, personal data export, or unredacted API response in a public issue.

The Skills read credentials from the local environment only. Revoke an exposed credential before reporting the incident.
